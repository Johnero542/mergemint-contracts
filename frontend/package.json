{
  "name": "mergemint-frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@playwright/test": "^1.45.0"
  }
}
